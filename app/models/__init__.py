# Phase 1: define your SQLAlchemy models here (User, Task, Label, TaskLabel,
# BlockingTask, TaskShare, Friendship) — ideally one file per model, all inheriting
# from app.db.base.Base. Import them here so Alembic autogenerate can see them.
